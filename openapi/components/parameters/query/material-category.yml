name: material-category
in: query
required: false
description: Filter based on the Material Categories a resource uses.
schema:
  type: array
  items: 
    type: string  
  minItems: 1
style: pipeDelimited