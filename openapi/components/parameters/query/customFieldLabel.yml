name: label
in: query
required: false
description: Filter custom field definitions based on the label parameter value.
schema:
  type: array
  items:
    type: string
  minItems: 1
style: pipeDelimited
