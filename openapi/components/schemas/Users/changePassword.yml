type: object
description: This is a definition of a change password request.
required:
  - oldPassword
  - newPassword
properties:
  oldPassword:
    description: The user's old password.
    type: string
  newPassword:
    description: The user's new password. Cannot be the same as the old password.
    type: string
