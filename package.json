{
  "name": "traceability-api-spec",
  "version": "0.0.1",
  "dependencies": {
    "@redocly/cli": "^1.0.0-beta.124",
    "graphql": "^16.6.0",
    "openapi-to-graphql": "^3.0.5",
    "openapi-to-graphql-cli": "^3.0.5",
    "spectaql": "^2.0.5"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o bundled && redocly bundle --format json -o bundled.json",
    "test": "redocly lint",
    "graphql": "openapi-to-graphql bundled.yml --save schema.graphql && npx spectaql spectaql.yml"
  }
}
